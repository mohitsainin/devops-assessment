name: Hello World CI

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: self-hosted
    steps:
      - name: Hello World Step
        shell: powershell
        run: |
          Write-Output "Hello World from Windows Self-Hosted Runner"
